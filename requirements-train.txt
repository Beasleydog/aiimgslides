# Colab / training dependencies.
datasets>=4,<5
trl>=0.24,<0.26
transformers>=4.57,<4.58
accelerate>=1.12,<2
peft>=0.18,<0.19
qwen-vl-utils>=0.0.14
Pillow>=10,<13
python-pptx>=1.0,<2

# Colab already ships a CUDA-matched torch. Keep this broad so pip does not
# try to replace it unless the runtime is missing torch.
torch>=2.10,<2.12
