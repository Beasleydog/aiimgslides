# Colab / training dependencies.
# Install after requirements.txt in a fresh runtime.
datasets>=3.6,<5
trl>=0.25,<0.26
transformers>=4.57,<4.58
accelerate>=1.12,<2
peft>=0.18,<0.19
bitsandbytes>=0.48,<0.49
qwen-vl-utils>=0.0.14

# Keep Unsloth and Unsloth Zoo aligned with current source builds for GRPO.
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
unsloth_zoo @ git+https://github.com/unslothai/unsloth-zoo.git

# Let Colab/PyTorch choose the matching CUDA wheel unless you intentionally
# install torch from a specific CUDA index before this file.
torch>=2.10,<2.12
